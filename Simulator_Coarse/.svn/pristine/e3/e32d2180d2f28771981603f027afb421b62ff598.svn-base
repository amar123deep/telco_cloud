import simpy

SIM_DURATION = 100

def population(env, ):

def world(env, scheduler):
    "A process that randomly spawns applications"

    while True:


env = simpy.Environment()


env.run(until=SIM_DURATION)